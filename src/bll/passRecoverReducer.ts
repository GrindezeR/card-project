const initialState = {}
export const passRecoverReducer = (state = initialState, action: PassRecoverActionsType) => {
    switch (action.type) {
        default:
            return state;
    }
}

export type PassRecoverActionsType = { type: string }